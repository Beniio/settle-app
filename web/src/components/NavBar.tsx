import { Box } from '@chakra-ui/layout';
import React from 'react';

interface NavBarProps {}

export const NavBar: React.FC<NavBarProps> = ({}) => {
  return (
    <Box bg="turquoise" p={4}>
      navbar
    </Box>
  );
};
